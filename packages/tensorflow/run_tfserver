#!/bin/bash

# cd "${0%/*}"
HTTP_PORT=${HTTP_PORT:-8080}
GRPC_PORT=${GRPC_PORT:-8080}
MODEL_DIR=${MODEL_DIR:-/models}

python3 -m tfserver.inferencer \
  --model_name=$MODEL_NAME \
  --model_dir=$MODEL_DIR \
  --http_port=$HTTP_PORT \
  --grpc_port=$GRPC_PORT \
  $@